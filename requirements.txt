# Runtime and tests. One file on purpose: a reviewer should need one command.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.12,<3
python-dotenv>=1.0

# The AI agent layer: guide-listed Pydantic AI with Google AI Studio (Gemini).
# The [google] extra installs the official google-genai provider dependency.
pydantic-ai-slim[google]==2.43.0

# Tests
pytest>=8.0
